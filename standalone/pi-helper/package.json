{
  "name": "@warpi/pi-agent-runtime",
  "version": "0.1.2",
  "private": true,
  "description": "Private stdio helper embedding the Pi coding-agent SDK for the standalone Warp agent backend. Not a public package.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "bin": {
    "warpi-pi-runtime": "dist/main.js"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.2",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@types/node": "24.6.0",
    "tsx": "4.23.1",
    "typescript": "5.9.3"
  }
}
